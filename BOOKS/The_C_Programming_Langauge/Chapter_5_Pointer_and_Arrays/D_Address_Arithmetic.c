/*---------------------------  5.4 Address Arithmetic -----------------------------------------*/

/*
- alloc(n)    : Returns a pointer p to n consecutive character positions.
- afree(p)    : Releases the storage thus acquired so it can be re-used later.
- Storaged managed to these are STACK : last-in, first-out list.

- allocp, allocbuf, afree info OMITTED
*/