/*---------------------------  1.1 Getting Started  -----------------------------------------*/

#include <stdio.h> // Include information about standard library..

main() // Program begins executing at the beginning of main
       // Define a function called main that recieves no argument values
{
    printf("hello, world\n");  // Main calls library function printf
                               // \n represents a new line character. Without it, produces an error
    printf("Hello, ");
    printf("World");
    printf("\n"); // Produces the same output
                  // \t for tab
                  // \b for backspace
                  // \* for double quote
                  // \\ for backslack
}