/*---------------------------  2.4 Declarations  -----------------------------------------*/

/*
- Can initialise a variable in its declaration
- If variable NOT automatic, Initialisation is done only once
*/

int strlen(const char[]);  //Specifies that the function does not change that array